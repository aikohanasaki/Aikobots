# STMB Parity Matrix

Reference oracle: `aikohanasaki/SillyTavern-MemoryBooks@8f21abc8de6079544333911bda3bb8cb9e112beb`

This matrix tracks audited STMB product functions against the current Aikobots implementation.

| Subsystem | STMB source file | STMB source function | Aikobots location | Status | Note |
| --- | --- | --- | --- | --- | --- |
| Config / Profiles | `index.js` | `initializeSettings` / `validateSettings` / `validateAndFixProfiles` | `public/scripts/stmb-core.js` `normalizeStmbSettings` | Partial | Steady-state core normalization now aligns for builtin/current-ST invariants, `maxTokens`, `tokenWarningThreshold`, `defaultMemoryCount`, `autoSummaryInterval`, summary/arc order syncing, and outlet-only `outletName` retention; legacy migration side effects in `index.js` are now treated as non-goal compatibility code, per-profile prompt overrides remain intentionally removed by user request, and end-to-end runtime verification is still pending. |
| Config / Profiles | `profileManager.js` / `utils.js` | `newProfile` / `editProfile` / `deleteProfile` / `exportProfiles` / `importProfiles` / `createProfileObject` | `public/scripts/stmb.js` profile editor/import-export helpers + `public/scripts/stmb-core.js` `sanitizeProfile` | Exact | Dedicated profile-manager create/edit/delete/export/import flow has now passed manual testing, including safe-name handling, duplicate-count import reporting, live temperature/model/reverse-start sanitization, required outlet names for Outlet placement, manual `Set As Default`, and outlet-only `outletName` persistence; per-profile prompt overrides remain intentionally removed by approved deviation. |
| Config / Profiles | `index.js` | `showRegexSelectionPopup` | `public/scripts/stmb.js` `showRegexSelectionPopup` | Exact | Popup-local semantics now match STMB, including option enumeration, select2 fallback, save behavior, and success/error toast handling. |
| Config / Profiles | `confirmationPopup.js` | `saveNewProfileFromAdvancedSettings` | `public/scripts/stmb.js` `saveAdvancedProfile` | Exact | Advanced save-profile creation now matches STMB's narrow field carryover semantics for name, preset, title format, and connection api/model/temperature only; the only intentional difference is the approved removal of profile custom-prompt persistence. |
| Scene System | `sceneManager.js` | marker toggle/update helpers | `public/scripts/stmb.js` scene marker helpers | Exact | Core marker placement/removal semantics match STMB closely. |
| Scene System | `index.js` | `handleSceneMemoryCommand` | `public/scripts/stmb.js` `sceneMemoryCommand` | Partial | Range parsing, scene-set messaging, and the launch handoff now go through a centralized `initiateMemoryCreation` path with STMB-style busy-state gating, character/group readiness checks, and `unhideBeforeMemory` preflight; final runtime verification is still pending. |
| Scene System | `index.js` | `handleNextMemoryCommand` | `public/scripts/stmb.js` `nextMemoryCommand` | Partial | Empty/no-new-message/lorebook preflight are closer; final runtime verification still needed. |
| Scene System | `index.js` | scene overlap guard in memory initiation | `public/scripts/stmb-core.js` `findOverlappingManagedMemoryEntry` + `public/scripts/stmb.js` `createMemoryFromRange` | Exact | Guard semantics now match STMB, including checking any `stmemorybooks` entry that carries `STMB_start` / `STMB_end`, detailed overlap diagnostics, and the same overlap condition/fail path. |
| Memory Generation | `index.js` | `createMemory` pipeline | `public/scripts/stmb.js` `createMemoryFromRange` | Partial | End-to-end pipeline is closer, but popup/UI parity is still incomplete, per-profile prompt overrides were intentionally removed by user request, and the main settings surface still does not expose STMB's summary-order controls that influence downstream consolidation. |
| Memory Generation | `stmemory.js` | prompt building / regex shaping | `src/endpoints/stmb.js` `prepare-memory-messages` + `public/scripts/stmb.js` `requestStructuredMemory` | Partial | Prompt preparation is mostly server-owned now, parsing still happens client-side, STMB max-token overrides are now reapplied on the client generate-data path, and runtime-only prompt edits now stay transient instead of persisting into profiles. |
| Memory Generation | `stmemory.js` | structured memory parse | `public/scripts/stmb-core.js` `parseStructuredMemoryResponse` | Partial | Parser behavior is close, but full provider/runtime validation is still open. |
| Memory Generation | `summaryPromptManager.js` | prompt preset file manager | `public/scripts/stmb-summary-prompt-manager.js` + `public/scripts/stmb.js` summary prompt manager helpers | Exact | File-backed preset management, built-in display-name resolution, first-run creation, import/export, recreate-builtins, popup lifecycle, localized popup semantics, and apply-to-profile behavior now match STMB; the only intentional difference is the approved removal of the profile custom-prompt clear branch. |
| Summaries | `arcAnalysisPromptManager.js` | arc prompt preset file manager | `public/scripts/stmb-arc-prompt-manager.js` + `public/scripts/stmb.js` `showArcPromptManagerPopup` | Partial | Consolidation prompts now use a dedicated file-backed `stmb-arc-prompts.json` cache with legacy settings migration, built-in rebuild support, and a local manager popup, and the stale settings-backed resolver has been removed; browser/runtime parity validation is still pending. |
| Memory Review / Repair | `confirmationPopup.js` | `showConfirmationPopup` | `public/scripts/stmb-popups.js` `showConfirmationPopup` | Partial | Main confirmation flow exists, but the full STMB settings entry point is still missing. |
| Memory Review / Repair | `confirmationPopup.js` | `showAdvancedOptionsPopup` | `public/scripts/stmb-popups.js` `showAdvancedOptionsPopup` | Partial | Dynamic save-and-create behavior is closer; broader profile CRUD surface is missing. |
| Memory Review / Repair | `confirmationPopup.js` | `showMemoryPreviewPopup` | `public/scripts/stmb-popups.js` `showMemoryPreviewPopup` | Partial | Preview/edit/retry/cancel flow is closer; final runtime parity still needs validation. |
| Memory Review / Repair | `index.js` | `showFailedAIResponsePopup` | `public/scripts/stmb-popups.js` `showFailedAIResponsePopup` | Partial | Repair flow exists, but the popup is not yet fully matched. |
| Lorebook Persistence | `addlore.js` | title numbering helpers | `public/scripts/stmb-core.js` `getNextManagedMemorySequenceNumber` | Exact | Number extraction, wrapped-token rendering, no-token title handling, repeated token replacement, and order derivation now follow STMB's numbering path. |
| Lorebook Persistence | `addlore.js` | managed memory entry creation | `src/endpoints/stmb.js` `save-memory` | Partial | Entry fields are closer to STMB; broader roundtrip verification is still needed. |
| Lorebook Persistence | `lorebookValidation.js` | `validateLorebookRequirement` | `public/scripts/stmb-lorebook.js` `ensureResolvedLorebookName` | Partial | Missing/unbound lorebooks now use a recovery popup flow instead of hard-failing. |
| Summaries | `summaryTiers.js` | tier definitions | `public/scripts/stmb-summary.js` tier helpers | Exact | Tier labels and mapping align with STMB. |
| Summaries | `arcanalysis.js` / `index.js` | summary generation pipeline | `public/scripts/stmb.js` `createSummaryForTier` + `runSequentialSummaryAnalysis` + `public/scripts/stmb-summary.js` `resolveSelectedSummarySourceEntries` | Partial | Core generation now supports selected-source filtering, token-budget trimming, parse-retry, multi-pass limits, commit, repair, and post-commit editor refresh more like STMB; browser/runtime validation is still pending. |
| Summaries | `index.js` | `showSummaryConsolidationPopup` | `public/scripts/stmb.js` `showSummaryConsolidationPopup` + `public/scripts/stmb-popups.js` `showSummaryConsolidationOptionsPopup` | Partial | The popup now preserves summary-entry settings, renders candidate checklists by tier, exposes max-items/token/max-pass controls, uses file-backed arc prompt presets, and still renders without a lorebook like STMB; dedicated manager UI parity is still missing. |
| Summaries | `index.js` | `showFailedSummaryResponsePopup` | `public/scripts/stmb-popups.js` `showFailedSummaryResponsePopup` | Partial | Repair popup is closer, but full surface parity is still open. |
| Summaries | `index.js` / `autosummary.js` | auto-summary trigger and postpone flow | `public/scripts/stmb.js` `checkAutoSummaryTrigger` / `resolveAutoSummaryLorebook` | Partial | Runtime trigger and manual-mode select/postpone are present; broader validation still needed. |
| Side Prompts / Trackers | `sidePromptsManager.js` | `findTemplateByName` | `public/scripts/stmb-sideprompts-manager.js` `findTemplateByName` | Exact | Loose name/key/slug lookup matches STMB behavior closely. |
| Side Prompts / Trackers | `sidePromptsManager.js` / `sidePromptsPopup.js` | manager CRUD/import/export/recreate | `public/scripts/stmb-sideprompts-manager.js` manager helpers + `public/scripts/stmb.js` `showSidePromptManagerPopup` | Partial | Popup behavior is now much closer to STMB, including table structure, empty-state rendering, inline actions, recreate/delete confirmation flow, editor affordances, outlet validation, import normalization messaging, and built-in key alignment for Cast. Remaining gate is browser/runtime verification. |
| Side Prompts / Trackers | `sidePrompts.js` | manual `/sideprompt` runner | `public/scripts/stmb-sideprompts.js` `runSidePrompt` | Partial | Manual flow now matches STMB more closely, including range tips, lorebook recovery, temporary unhide/restore compile behavior, and active chat-context metadata for group/manual state; broader runtime verification is still pending. |
| Side Prompts / Trackers | `sidePrompts.js` | interval tracker runner | `public/scripts/stmb-sideprompts.js` `evaluateTrackers` | Partial | Checkpoint, preview, token-override, editor-refresh, and temporary unhide/restore compile behavior are closer; Aikobots intentionally deviates from STMB by regenerating immediately when a user clicks preview `Retry`, and broader runtime verification is still pending. |
| Side Prompts / Trackers | `sidePrompts.js` | after-memory wave runner | `public/scripts/stmb-sideprompts.js` `runAfterMemory` + `src/endpoints/stmb.js` `/upsert-entries-batch` | Partial | Concurrent waves, receipt-order previews, abort-aware per-attempt generation, preview-failure fallback, token overrides, batched wave saves, and editor refresh are closer; broader runtime validation is still pending. |
| Side Prompts / Trackers | `sidePrompts.js` | preview serialization | `public/scripts/stmb-sideprompts.js` `enqueuePreview` / `resolveSidePromptPreview` | Partial | Serialized previews now exist for side prompts like STMB. |
| Slash Commands | `index.js` | `registerSlashCommands` | `public/scripts/stmb.js` `registerSlashCommands` | Exact | Command set, names, argument shapes, and handler wiring now match STMB. |
| Slash Commands | `index.js` | sideprompt autocomplete providers | `public/scripts/stmb.js` sideprompt enum providers | Partial | Name and macro suggestions now exist and are closer to STMB. |
| Slash Commands | `index.js` | `handleHighestMemoryProcessedCommand` | `public/scripts/stmb.js` `getHighestProcessedCommand` | Exact | Return semantics match STMB's direct stringification of the current highest processed message. |
| Slash Commands | `index.js` | `handleSetHighestMemoryProcessedCommand` | `public/scripts/stmb.js` `setHighestProcessedCommand` | Partial | Error/success/clamp semantics now match closely; remaining uncertainty is whether local popup refresh breadth fully matches STMB's full refresh path in every open-settings state. |
| Slash Commands | `index.js` | `handleStmbStopCommand` | `public/scripts/stmb.js` `stopStmbCommand` | Partial | Stop wording, in-flight detection, toast clearing, and popup closing align; remaining gate is runtime validation that task-abort coverage fully substitutes for STMB's legacy busy-flag resets. |
| UI / Entry Points | `index.js` | `createUI` / `showSettingsPopup` | `public/scripts/stmb.js` `createMainEntryUi` / `showMainEntryPopup` | Exact | Main Memory Books menu entry and settings popup now pass manual testing for the supported settings surface; the bottom-of-popup layout differs from STMB only by approved deviation. |
| Architecture / Ownership | `index.js` + `confirmationPopup.js` + `sidePrompts.js` | client orchestration + lorebook writes | `public/scripts/stmb.js`, `public/scripts/stmb-sideprompts.js`, `src/endpoints/stmb.js` | Partial | Prompt preparation and writes are mostly server-owned, and the stale settings-backed arc prompt resolver has been removed; client still owns significant orchestration and some parse/error handling. |
